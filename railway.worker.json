{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build -w @scheduler/shared && npm run build -w @scheduler/worker"
  },
  "deploy": {
    "startCommand": "npm run start -w @scheduler/worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
